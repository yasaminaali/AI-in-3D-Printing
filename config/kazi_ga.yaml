machine_id: kazi_ga
num_workers: 4
output_dir: output/kazi_ga
dataset_jsonl: "datasets/80x80_complete.jsonl"

assignments:
  - grid: [80, 80]
    patterns: [stripes, islands, voronoi, left_right]
    ga_config: default
    genome_len:
      stripes: 850
      islands: 800
      voronoi: 900
      left_right: 700
